// Export — Download current active data as .xlsx: complete database, price list, test catalog, profiles, change history.
export default function ExportPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Export</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Download current active data as .xlsx: complete database, price list, test catalog, profiles, change history.
      </p>
    </div>
  );
}
