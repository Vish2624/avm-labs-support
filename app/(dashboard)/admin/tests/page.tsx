// Tests — Manage the master test catalog (add/update tests, categories, descriptions).
export default function TestsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Tests</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Manage the master test catalog (add/update tests, categories, descriptions).
      </p>
    </div>
  );
}
