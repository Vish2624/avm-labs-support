// Admin overview. Real content (recent imports, quick links) lands once the
// sections below are built out (Phases 6-7).
export default function AdminOverviewPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Admin</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Price list uploads, catalog/alias/profile management, and import history.
      </p>
    </div>
  );
}
