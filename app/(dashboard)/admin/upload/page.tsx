// Upload — Upload a location's price list Excel file. Feeds into staging -> validate -> preview -> confirm -> activate (Phase 6).
export default function UploadPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Upload</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Upload a location&apos;s price list Excel file. Feeds into staging → validate → preview → confirm → activate (Phase 6).
      </p>
    </div>
  );
}
