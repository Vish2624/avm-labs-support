// Profiles — Manage profiles/packages and which tests belong to each.
export default function ProfilesPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Profiles</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Manage profiles/packages and which tests belong to each.
      </p>
    </div>
  );
}
