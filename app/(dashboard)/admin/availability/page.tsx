// Availability — Toggle test/profile availability per location + service type without a full re-upload.
export default function AvailabilityPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Availability</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Toggle test/profile availability per location + service type without a full re-upload.
      </p>
    </div>
  );
}
