// Support Workspace — the single page an agent uses to search, price, quote,
// and generate a WhatsApp reply. Built in Phase 4, once schema/auth/search
// are in place. See components/workspace/* for the piece placeholders.
export default function WorkspacePage() {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold">Support Workspace</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Search, quotation, profile suggestions, and WhatsApp response generation land here.
      </p>
    </main>
  );
}
