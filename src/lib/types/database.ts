/**
 * Placeholder for Supabase-generated database types.
 *
 * Once migrations are applied (Phase 1), regenerate with:
 *   npx supabase gen types typescript --project-id <ref> > src/lib/types/database.ts
 *
 * Until then this is intentionally loose so the rest of the app can compile
 * against `Database` without blocking on a live project.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
