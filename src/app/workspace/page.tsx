export default function WorkspacePage() {
  return (
    <main className="flex-1 flex flex-col items-center justify-center p-8">
      <div className="text-center space-y-2">
        <h1 className="text-xl font-semibold">Support Workspace</h1>
        <p className="text-muted-foreground text-sm max-w-md">
          Search, pricing, quotation, and WhatsApp message generation land here in Phase 4,
          once the schema (Phase 1), auth (Phase 2), and search engine (Phase 3) are in place.
        </p>
      </div>
    </main>
  );
}
