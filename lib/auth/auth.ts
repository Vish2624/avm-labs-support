/**
 * TODO: Read the current auth session (admin login) server-side.
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function auth(): never {
  throw new Error("Not implemented: auth");
}
