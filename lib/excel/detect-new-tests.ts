/**
 * TODO: Flag staged rows whose test code doesn't exist in the master catalog yet.
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function detectNewTests(): never {
  throw new Error("Not implemented: detectNewTests");
}
