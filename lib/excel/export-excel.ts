/**
 * TODO: Build an .xlsx workbook from current database state (never from the last uploaded file).
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function exportExcel(): never {
  throw new Error("Not implemented: exportExcel");
}
