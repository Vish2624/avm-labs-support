/**
 * TODO: Diff staged rows against the currently active version (price/TAT/availability changes, large-change warnings).
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function detectChanges(): never {
  throw new Error("Not implemented: detectChanges");
}
