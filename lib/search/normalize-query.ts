/**
 * TODO: Lowercase/trim/strip punctuation from a raw search query before matching.
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function normalizeQuery(): never {
  throw new Error("Not implemented: normalizeQuery");
}
