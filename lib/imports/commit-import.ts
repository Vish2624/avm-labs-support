/**
 * TODO: Transactionally activate a validated import and archive the previous version.
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function commitImport(): never {
  throw new Error("Not implemented: commitImport");
}
