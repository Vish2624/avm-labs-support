/**
 * TODO: Run full validation for a staged import; block on errors, flag warnings.
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function validateImport(): never {
  throw new Error("Not implemented: validateImport");
}
