/**
 * TODO: Format a quotation's verified DB fields into a copy-ready WhatsApp message.
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function generateResponse(): never {
  throw new Error("Not implemented: generateResponse");
}
