/**
 * TODO: Sum line items into a quotation total using safe money arithmetic (see money.ts).
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function calculateQuotation(): never {
  throw new Error("Not implemented: calculateQuotation");
}
