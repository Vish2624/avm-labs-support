/**
 * TODO: Zod schema for one parsed Excel import row.
 * Placeholder only — not implemented yet (see AVM_PLAN.md).
 */
export function importSchema(): never {
  throw new Error("Not implemented: importSchema");
}
