// TODO: Single test search result row (code, name, price, TAT, availability).
// Placeholder only — see AVM_PLAN.md for the phase this gets implemented in.
export function TestResultCard() {
  return null;
}
